# Agent State Log

## Current status (update this every session)
- Dev server: running locally at `localhost:3000` (`pnpm dev`, background process)
- Last verified working: `pnpm install` + `pnpm check` (tsc --noEmit) clean; dev server boots and serves login/dashboard/fleet routes
- Known stubs / mocks: Login previously accepted any input; now gated to `admin` / `admin123` (still client-side only, no real backend auth) — see `client/src/App.tsx` `LoginPage`
- In progress / uncommitted:
  - `package.json` — added `pnpm.onlyBuiltDependencies: ["@tailwindcss/oxide", "esbuild"]` so `pnpm install` runs their postinstall scripts non-interactively (pnpm was blocking them by default)
  - `client/src/App.tsx` — `LoginPage` now requires `admin` / `admin123` to navigate to `/dashboard`; anything else shows an "Invalid email or password" toast (Enter key also submits)
  - Neither change is committed yet — not committing without explicit user request per session convention
- Next suggested step: review and commit the two pending changes above if acceptable; otherwise decide on a real auth approach before treating login as anything but a stub

## Session log (newest first)
### 2026-09-16 — Claude Code
- Task: install project deps, run dev server for UI/UX review, pull latest `origin/main` (new hero image asset), then gate the login form to a dummy admin/admin123 account
- Changed:
  - `package.json` (pnpm build-script allowlist)
  - `client/src/App.tsx` (`LoginPage` credential check)
  - Pulled `daf5076` from `origin/main` (adds `client/public/assets/wingbox-aircraft-hero.jpg`, small `App.tsx`/`index.css` tweaks) — fast-forward, no conflicts
- Commits: none yet — both `package.json` and `App.tsx` changes are uncommitted in the working directory
- Notes for next agent: `pnpm` was not installed/available on this machine at session start; installed globally via `npm install -g pnpm@10.15.1` (resolved to project's pinned `10.4.1`). If your environment already has pnpm, skip that. This is the first entry in this log — no prior handoff to reconcile against.
